services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      - mongo
    volumes:
      - .:/app
    command: ["python", "bot.py"]
    networks:
      - innovision

  mongo:
    image: mongo:6
    restart: always
    # ports:
    #   - "12922:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - innovision

  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    env_file:
      - .env
    expose:
      - "5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - innovision


volumes:
  pgdata:
  mongo_data:

networks:
  innovision:
    driver: bridge